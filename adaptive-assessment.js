(()=>{
 const KEY='wonder_assessment_v2';
 const getState=()=>{try{return JSON.parse(localStorage.getItem('wonder_preview_state')||'{}')}catch{return{}}};
 const getA=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return{}}};
 const saveA=a=>localStorage.setItem(KEY,JSON.stringify(a));
 const A=Object.assign({responses:{},history:[],sessionId:null,complete:false,cache:{},changedCounts:{}},getA());
 let current=null,selected=null,currentMeta={},booted=false,questionShownAt=Date.now();
 const $=id=>document.getElementById(id),authToken=()=>getState()?.auth?.accessToken||null,headers=()=>{const h={'Content-Type':'application/json'};const t=authToken();if(t)h.Authorization=`Bearer ${t}`;return h};
 const ELEMENT_COPY={Earth:'Reality · foundations',Water:'Emotion · attachment',Fire:'Desire · vitality',Air:'Mind · worldview',Ether:'Meaning · identity'};
 async function startSession(){if(!authToken())throw new Error('Sign in is required before the questionnaire can begin.');const r=await fetch('/api/assessment/start',{method:'POST',headers:headers(),body:JSON.stringify({questionnaire_version:'wonder-questionnaire-v2.2-elements'})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Wonder could not start your assessment.');const id=d.session?.id||null;if(id){const changed=A.sessionId&&A.sessionId!==id;A.sessionId=id;A.responses=d.responses||{};A.complete=false;A.snapshotSyncedAt=null;if(changed||d.resumed){A.history=[];A.cache={}}saveA(A)}}
 async function persist(itemId,response){if(!A.sessionId||!authToken())return true;const r=await fetch('/api/assessment/respond',{method:'POST',headers:headers(),body:JSON.stringify({session_id:A.sessionId,item_id:itemId,response,response_time_ms:Math.max(0,Date.now()-questionShownAt),changed_count:Number(A.changedCounts?.[itemId]||0)})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Wonder could not save that response.');return true}
 async function chooseNext(){const r=await fetch('/api/assessment/next',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({responses:A.responses})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Wonder could not choose the next question.');return d}
 const phaseLabel=m=>m?.element?`${m.element} · ${ELEMENT_COPY[m.element]||''}`:'Assessment';const setProgress=(c,t=36)=>{if($('progressBar'))$('progressBar').style.width=Math.min(96,Math.max(4,(c/Math.max(35,t))*92))+'%'};const optionButton=(l,i,a=false)=>`<button class="option ${a?'selected':''}" data-i="${i}">${l}</button>`;
 function render(item,meta={},preserveTimer=false){if(!item)throw new Error('Wonder received an empty questionnaire item.');current=item;currentMeta=meta;selected=A.responses[item.id]??null;if(!preserveTimer)questionShownAt=Date.now();A.cache[item.id]={item,meta};saveA(A);const element=meta.element||'Earth';if($('sectionLabel'))$('sectionLabel').textContent=phaseLabel(meta);if($('assessment'))$('assessment').dataset.element=element.toLowerCase();setProgress(meta.count||Object.keys(A.responses).length,meta.target_max||36);let body='';if(item.type==='single')body=`<div class="options">${item.options.map((o,i)=>optionButton(o.label,i,Number(selected)===i)).join('')}</div>`;if(item.type==='scale'){const n=Number(selected||0);body=`<div class="scale-wrap"><div class="scale-anchors"><span>${item.anchors?.[0]||'Not at all'}</span><span>${item.anchors?.[1]||'Extremely'}</span></div><div class="scale-options">${[1,2,3,4,5,6,7].map(v=>`<button class="scale-option ${n===v?'selected':''}" data-v="${v}">${v}</button>`).join('')}</div></div>`}if(item.type==='multi'){const arr=Array.isArray(selected)?selected:[];body=`<div class="question-helper">Choose up to ${item.max||3}.</div><div class="options">${item.options.map((o,i)=>optionButton(o.label,i,arr.includes(i))).join('')}</div>`}if(item.type==='rank'){const arr=Array.isArray(selected)?selected:[];body=`<div class="question-helper">Choose ${item.max||5} in priority order. Tap again to remove.</div><div class="rank-summary">${arr.length?arr.map((i,r)=>`<span>${r+1}. ${item.options[i].label}</span>`).join(''):'Your ranking will appear here.'}</div><div class="options compact-options">${item.options.map((o,i)=>optionButton(o.label,i,arr.includes(i))).join('')}</div>`}$('questionMount').innerHTML=`<div class="element-marker"><span>${String(meta.element_index||1).padStart(2,'0')}</span><b>${element}</b><small>${ELEMENT_COPY[element]||''}</small></div><div class="question-title">${item.prompt}</div>${body}`;bindInputs(item);$('prevQuestion').style.visibility=A.history.length?'visible':'hidden';$('nextQuestion').style.visibility='visible';$('nextQuestion').textContent='Continue';updateContinue(item)}
 function rerenderSelection(item){const keep=selected;render(item,currentMeta,true);selected=keep;if(item.type==='single')$('questionMount').querySelectorAll('.option').forEach(el=>el.classList.toggle('selected',Number(el.dataset.i)===Number(keep)));if(item.type==='scale')$('questionMount').querySelectorAll('.scale-option').forEach(el=>el.classList.toggle('selected',Number(el.dataset.v)===Number(keep)));if(item.type==='multi'||item.type==='rank'){const arr=Array.isArray(keep)?keep:[];$('questionMount').querySelectorAll('.option').forEach(el=>el.classList.toggle('selected',arr.includes(Number(el.dataset.i))));if(item.type==='rank'){const s=$('questionMount').querySelector('.rank-summary');if(s)s.innerHTML=arr.length?arr.map((i,r)=>`<span>${r+1}. ${item.options[i].label}</span>`).join(''):'Your ranking will appear here.'}}selected=keep;updateContinue(item);saveA(A)}
 function bindInputs(item){if(item.type==='single')$('questionMount').querySelectorAll('.option').forEach(el=>el.onclick=()=>{selected=Number(el.dataset.i);rerenderSelection(item)});if(item.type==='scale')$('questionMount').querySelectorAll('.scale-option').forEach(el=>el.onclick=()=>{selected=Number(el.dataset.v);rerenderSelection(item)});if(item.type==='multi')$('questionMount').querySelectorAll('.option').forEach(el=>el.onclick=()=>{let a=Array.isArray(selected)?[...selected]:[];const i=Number(el.dataset.i);a=a.includes(i)?a.filter(x=>x!==i):(a.length<(item.max||3)?[...a,i]:a);selected=a;rerenderSelection(item)});if(item.type==='rank')$('questionMount').querySelectorAll('.option').forEach(el=>el.onclick=()=>{let a=Array.isArray(selected)?[...selected]:[];const i=Number(el.dataset.i);a=a.includes(i)?a.filter(x=>x!==i):(a.length<(item.max||5)?[...a,i]:a);selected=a;rerenderSelection(item)})}
 const valid=item=>(item.type==='single'||item.type==='scale')?selected!==null&&selected!==undefined:item.type==='multi'?Array.isArray(selected)&&selected.length>0:item.type==='rank'?Array.isArray(selected)&&selected.length===(item.max||5):false;function updateContinue(item){const b=$('nextQuestion');if(b){b.disabled=!valid(item);b.style.opacity=valid(item)?'1':'.45'}}
 async function next(){if(!current||!valid(current))return;const b=$('nextQuestion');b.disabled=true;b.textContent='Saving…';try{await persist(current.id,selected);A.responses[current.id]=selected;if(A.history.at(-1)!==current.id)A.history.push(current.id);saveA(A);const n=await chooseNext();if(n.complete){await finish();return}render(n.item,n)}catch(e){$('questionMount').insertAdjacentHTML('beforeend',`<p class="muted assessment-error">${String(e.message||'Wonder lost the thread for a moment.')}</p>`);b.textContent='Continue';updateContinue(current)}}
 function back(){if(!A.history.length)return;const target=A.history.pop(),cached=A.cache?.[target];saveA(A);if(cached?.item)render(cached.item,cached.meta||{})}
 const names={cognitive_systemizing:'structured thinking',cognitive_contextual:'context-sensitive thinking',ambiguity_tolerance:'comfort with ambiguity',decisiveness:'decisiveness',autonomy_need:'autonomy',closeness_need:'closeness',repair_orientation:'repair',reciprocity_sensitivity:'reciprocity',trust_baseline:'baseline trust',value_family:'family',value_achievement:'achievement',value_meaning:'meaning',value_freedom:'freedom',value_stability:'stability',value_knowledge:'knowledge',value_service:'service',value_influence:'influence',value_beauty:'beauty',value_loyalty:'loyalty'};const dominant=(d,keys)=>keys.map(k=>[k,d[k]||0]).sort((a,b)=>b[1]-a[1])[0];function fallbackMirror(model,archetypes){const d=model.dimensions||{},primary=archetypes[0],secondary=archetypes[1],cog=dominant(d,['cognitive_systemizing','cognitive_contextual','ambiguity_tolerance','decisiveness']),rel=dominant(d,['autonomy_need','closeness_need','repair_orientation','reciprocity_sensitivity','trust_baseline']),val=dominant(d,['value_family','value_achievement','value_meaning','value_freedom','value_stability','value_knowledge','value_service','value_influence','value_beauty','value_loyalty']);return{primary,secondary,move:`Your responses lean toward ${names[cog[0]]}.`,drive:`Of the values Wonder measured, ${names[val[0]]} currently carries the strongest signal.`,relationship:`In relationships, ${names[rel[0]]} stands out.`,tension:'Wonder is still looking for the most meaningful tension in your profile.'}}
 function renderMirror(out){const m=out.mirror||fallbackMirror(out.model||{},out.archetypes||[]),primary=m.primary||out.archetypes?.[0],secondary=m.secondary||out.archetypes?.[1];$('archetypeName').textContent='The '+(primary?.name||'Unfolding');$('archetypeNote').textContent=`Primary archetype${secondary?.name?` · ${secondary.name} influence`:''}`;$('mirrorMoveTitle').textContent=m.move_title||primary?.essence||'How you make sense of things';$('mirrorMove').textContent=m.move||'';$('mirrorDriveTitle').textContent=m.drive_title||'What carries weight';$('mirrorDrive').textContent=m.drive||'';$('mirrorRelTitle').textContent=m.relationship_title||'How connection works for you';$('mirrorRel').textContent=m.relationship||'';$('mirrorShadowTitle').textContent=m.tension_title||'A tension Wonder noticed';$('mirrorShadow').textContent=m.tension||'';$('mirrorUncertain').textContent='The Mirror looks for patterns across your choices. It remains open to correction.'}
 async function finish(){// Never create a Mirror from an empty or partial accidental state.
   const count=Object.keys(A.responses||{}).length;if(count<35){booted=false;current=null;const n=await chooseNext();if(n.item){render(n.item,n);return}throw new Error('The questionnaire is not complete yet.');}
   $('progressBar').style.width='100%';$('sectionLabel').textContent='Mirror · pattern beneath self-report';$('questionMount').innerHTML='<div class="question-title">The elements gathered the signals. The Mirror reveals the pattern.</div>';$('nextQuestion').style.visibility='hidden';$('prevQuestion').style.visibility='hidden';try{let out=null;if(A.sessionId&&authToken()){const r=await fetch('/api/assessment/complete',{method:'POST',headers:headers(),body:JSON.stringify({session_id:A.sessionId})});const d=await r.json().catch(()=>({}));if(r.ok)out=d;else throw new Error(d.error||'Wonder could not finish the portrait yet.')}if(!out)throw new Error('A completed signed-in assessment is required.');A.complete=true;A.result=out;saveA(A);const s=getState();s.archetype=(out.mirror?.primary?.name||out.archetypes?.[0]?.name||'Unfolding');s.assessmentV2=out;localStorage.setItem('wonder_preview_state',JSON.stringify(s));renderMirror(out);setTimeout(()=>window.show?window.show('mirror'):null,700)}catch(e){$('questionMount').innerHTML=`<div class="question-title">Your answers are safe, but Wonder could not finish the portrait yet.</div><p class="muted">${String(e.message||'Try again in a moment.')}</p>`}}
 async function boot(){if(booted&&current)return;booted=true;try{await startSession();const n=await chooseNext();if(n.complete){await finish();return}render(n.item,n)}catch(e){booted=false;$('questionMount').innerHTML=`<div class="question-title">Wonder could not begin the assessment.</div><p class="muted">${String(e.message||'Try again in a moment.')}</p>`}}
 const nextBtn=$('nextQuestion'),prevBtn=$('prevQuestion');if(nextBtn)nextBtn.onclick=next;if(prevBtn)prevBtn.onclick=back;window.startWonderAssessment=boot;document.addEventListener('wonder:assessment-enter',boot);const observer=new MutationObserver(()=>{if($('assessment')?.classList.contains('active'))boot()});if($('assessment'))observer.observe($('assessment'),{attributes:true,attributeFilter:['class']});if($('assessment')?.classList.contains('active'))boot();
})();