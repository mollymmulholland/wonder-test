(()=>{const elementByScreen={welcome:'air',account:'earth',birth:'earth',essentials:'earth',visual:'air',assessment:'water',processing:'water',mirror:'water',home:'air',profile:'earth',journal:'earth',ai:'water',matches:'fire'};document.querySelectorAll('.screen').forEach(s=>s.dataset.element=elementByScreen[s.id]||'earth');const mark=document.createElement('div');mark.className='labyrinth-mark';mark.setAttribute('aria-hidden','true');mark.innerHTML='<i></i>';document.body.appendChild(mark);
const originalShow=window.show;if(typeof originalShow==='function'){window.show=function(id){const current=document.querySelector('.screen.active');if(!current||current.id===id)return originalShow(id);current.classList.add('wonder-leaving');setTimeout(()=>{current.classList.remove('wonder-leaving');originalShow(id)},360)}}
function installPool(){const mirror=document.getElementById('mirror');if(!mirror||mirror.dataset.poolInstalled)return;mirror.dataset.poolInstalled='true';const existing=mirror.querySelector('.mirror');if(!existing)return;const gate=document.createElement('div');gate.className='pool-entry';gate.innerHTML='<div class="pool-copy"><div class="eyebrow">The Pool</div><h1>Look beneath the surface.</h1><p class="lede" style="margin-left:auto;margin-right:auto">You have told Wonder who you think you are. Now enter what it is beginning to see.</p></div><div class="pool-surface" role="button" tabindex="0" aria-label="Enter your Mirror"></div><span class="pool-hint">Touch the water</span>';existing.style.display='none';mirror.insertBefore(gate,existing);let touched=false;const enter=()=>{const pool=gate.querySelector('.pool-surface');if(!touched){touched=true;pool.classList.add('ripple');gate.querySelector('.pool-hint').textContent='Touch again to descend';setTimeout(()=>pool.classList.remove('ripple'),1450);return}gate.classList.add('descend');setTimeout(()=>{gate.style.display='none';existing.style.display='block';existing.animate([{opacity:0,filter:'blur(14px)',transform:'translateY(28px)'},{opacity:1,filter:'blur(0)',transform:'translateY(0)'}],{duration:1200,easing:'cubic-bezier(.16,1,.3,1)',fill:'both'})},1050)};const pool=gate.querySelector('.pool-surface');pool.addEventListener('click',enter);pool.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();enter()}})}
installPool();
const mount=document.getElementById('questionMount');if(mount){new MutationObserver(()=>{const q=mount.querySelector('.question-title');if(q)q.animate([{opacity:0,transform:'translateY(14px)',filter:'blur(5px)'},{opacity:1,transform:'translateY(0)',filter:'blur(0)'}],{duration:650,easing:'cubic-bezier(.16,1,.3,1)'})}).observe(mount,{childList:true})}
})();