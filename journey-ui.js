/* Consumer journey. Synthetic state stays in this tab; live changes use authenticated APIs. */
window.WonderJourneyUI=function(ctx){
'use strict';
const {state,esc,mount,page,heading,btn,arrow,eye,go,api,experience,toast,error,statusEl,confirm}=ctx,J=window.WonderJourney;
const demoKey='wonder_journey_demo_v2';let world=null,own=null,owner=null,sectionId='portrait',tab='overview',pendingCorrection=null,selectedContext=null,writeDraft=null,saveTimer=null,saveChain=Promise.resolve(),dirty=false,mirrorMessages=[],pair=null,lastError='',scenario='normal',liveEnabled=false;
const paths={home:'M3 11 12 3l9 8M5 10v11h5v-7h4v7h5V10',report:'M3 12s3-7 9-7 9 7 9 7-3 7-9 7-9-7-9-7Zm9-3a3 3 0 1 0 0 6 3 3 0 0 0 0-6',introductions:'M8 4a3 3 0 1 0 0 6 3 3 0 0 0 0-6Zm8 0a3 3 0 1 0 0 6 3 3 0 0 0 0-6M2 21v-4a6 6 0 0 1 12 0v4m1-9a6 6 0 0 1 7 5v4',journal:'M5 3h14v18H5V3Zm4 5h6m-6 4h6m-6 4h4',settings:'M12 3a4 4 0 1 0 0 8 4 4 0 0 0 0-8ZM4 21v-3a8 8 0 0 1 16 0v3'};
const vector=p=>`<svg class="ui-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.3" stroke-linejoin="round" aria-hidden="true"><path d="${p}"/></svg>`;
const control=(label,action,extra='',secondary=false)=>`<button type="button" class="${secondary?'text-btn':'btn'}" data-w-action="${action}" ${extra}>${label}</button>`;
const field=(label,name,value='',type='text',attrs='')=>`<label class="field">${label}<input name="${name}" type="${type}" value="${esc(value)}" ${attrs}></label>`;
const area=(label,name,value='',attrs='')=>`<label class="field">${label}<textarea name="${name}" ${attrs}>${esc(value)}</textarea></label>`;
const check=(label,name,value)=>`<label class="check-label"><input type="checkbox" name="${name}" ${value?'checked':''}><span>${label}</span></label>`;
const select=(label,name,values,value)=>`<label class="field">${label}<select name="${name}">${values.map(([v,l])=>`<option value="${v}" ${v===value?'selected':''}>${l}</option>`).join('')}</select></label>`;
const notice=t=>`<div class="notice">${t}</div>`;
function download(name,data,type='application/json'){const blob=new Blob([typeof data==='string'?data:JSON.stringify(data,null,2)],{type});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);}
function storeDemo(){try{sessionStorage.setItem(demoKey,JSON.stringify(world));}catch{throw new Error('Not saved: this tab has run out of storage. Export your writing before resetting.');}}
async function load(){const key=state.mode==='demo'?'demo':state.user?.id;if(owner===key&&own)return;if(key==='demo'){try{world=JSON.parse(sessionStorage.getItem(demoKey));}catch{}if(!world?.users)world=J.fixture();own=world.users[world.actor];if(world.reports?.[world.actor])state.report=world.reports[world.actor];if(world.assessments?.[world.actor])state.assessment=world.assessments[world.actor];pair=world.pair;scenario=world.scenario||'normal';state.name=own.basics.name;}else {const d=await api('/api/journey',{action:'load'});own=d.state;pair=d.connection||null;liveEnabled=d.live_introductions===true;}owner=key;applyDisplay();}
function applyDisplay(){document.documentElement.classList.toggle('large-text',!!own?.display.largeText);document.documentElement.classList.toggle('quiet-motion',!!own?.display.reducedMotion);}
async function change(type,payload={}){await load();const event={type,payload};if(state.mode==='demo'){const next=J.privateEvent(own,event);world.users[world.actor]=next;storeDemo();own=next;}else{const d=await api('/api/journey',{action:'change',expected_version:own.version,event});own=d.state;}applyDisplay();return own;}
async function connect(type,payload={}){await load();if(state.mode==='demo'){if(scenario==='failed')throw new Error('Simulated service failure. Nothing was sent. Choose Normal in demo scenarios and retry.');world.pair=J.pairEvent(world.pair,{type,payload},world.actor,world.users);storeDemo();pair=world.pair;}else{const d=await api('/api/journey',{action:'connection',id:pair?.id,expected_version:pair?.version,event:{type,payload}});pair=d.connection;}return pair;}
function demoTools(){if(state.mode!=='demo')return '';return `<details class="demo-tools"><summary>Demonstration controls</summary><p class="small">Two fictional adults. Actions and written sample responses remain in this tab. No real messages, payments, notifications, or learning.</p><div class="actions">${control('Use '+(world.actor==='alex'?'Rowan':'Alex')+'’s perspective','switch','','true')}${control('Start with discovery','fresh','','true')}${control('Reset demonstration','reset','','true')}</div>${select('Scenario','demo-scenario',[['normal','Normal journey'],['empty','No eligible introduction'],['unavailable','Availability changed'],['declined','Declined'],['cancelled','Date cancelled'],['failed','Service failure'],['blocked','Blocked']],scenario)}</details>`;}
function shell(kicker,title,body,aside=''){mount(page(`${heading(kicker,title,aside)}${body}${statusEl()}${demoTools()}`));const h=document.querySelector('#main h1');if(h){h.tabIndex=-1;h.focus({preventScroll:true});}}
function nav(){const active=['ai','review'].includes(state.screen)?'report':['portrait','basics'].includes(state.screen)?'settings':state.screen;return `<nav class="nav" aria-label="Main navigation">${[['home','Home'],['report','Mirror'],['introductions','Introductions'],['journal','Journal'],['settings','Account']].map(([s,l])=>`<button data-action="${s}" class="${active===s?'active':''}" ${active===s?'aria-current="page"':''}>${vector(paths[s])}<span>${l}</span></button>`).join('')}</nav>`;}
function nextAction(){if(!own.consent.process||!own.basics.dob)return ['Begin with the essentials','Set up your private space','basics'];if(state.assessment&&!state.assessment.complete)return ['Your discovery is saved','Continue discovery','assessment'];if(!state.report&&!(state.mode==='demo'&&own.mirrorReviewed))return ['A thoughtful starting point','Begin discovery','assessment'];if(!own.mirrorReviewed)return ['Your first Mirror is ready','Review your Mirror','report'];if(!own.portrait.approved)return ['Choose what another person can see','Prepare your Connection Portrait','portrait'];if(own.availability!=='available')return ['Introductions are paused','Manage your availability','settings'];const p=viewPair();if(p?.plan?.status==='confirmed')return ['You have a confirmed plan','View your meeting','introductions'];if(p?.status==='mutual')return ['You are both interested','Open your conversation','introductions'];if(p?.status==='pending'&&p.myInterest)return ['Your interest is recorded','View your introduction','introductions'];if(p&&['proposed','pending'].includes(p.status))return ['An introduction to consider','Read the introduction','introductions'];return ['Understanding before introduction','View introduction status','introductions'];}
function home(){const [title,label,action]=nextAction();shell('Home',`A little more <em>understood.</em>`,`<div class="home-focus"><section><p class="eyebrow">What matters now</p><h2>${esc(title)}</h2><p>Welcome, ${esc(own.basics.name||state.name||'you')}. There is room to be specific, to reconsider, and to take your time.</p><div class="actions">${['assessment'].includes(action)?btn(label,action):control(label,action)}</div></section>${ctx.pool('Understanding.<br><em>Before introduction.</em>',false)}</div><section class="quiet-prompt"><p class="eyebrow">One question, if you want it</p><h3>When did you feel most like yourself?</h3>${control('Give the thought a little room','journal','','true')}</section>`);}
function basics(){shell('Your private space','First, <em>the essentials.</em>',`<div class="reading-column"><p>Your full birth date stays private. Your approved Connection Portrait can show your age and broad city.</p><form id="w-basics" class="form-grid">${field('Chosen name','name',own.basics.name,'text','required maxlength="80"')}${field('Date of birth','dob',own.basics.dob,'date','required')}${field('City or broad location','city',own.basics.city,'text','required maxlength="120"')}<div class="full">${check('WONDER may process my discovery answers to prepare my private Mirror. Journals are not interpreted automatically.','process',own.consent.process)}<p class="small muted">Account verification confirms access to your email; it is not identity verification. Authorized service operators may access records to run and support the service.</p><button class="btn">Save and continue</button></div></form></div>`);}
function how(){shell('How WONDER works','Understanding comes <em>first.</em>',`<div class="reading-column"><ol class="steps"><li><h3>Discover yourself.</h3><p>Move through five elements at your own pace. Your answers form a starting point.</p></li><li><h3>Review your Mirror.</h3><p>Read what is supported, notice what remains unclear, and correct what does not fit.</p></li><li><h3>Consider an introduction.</h3><p>Choose your shared portrait and availability. Introductions depend on mutual preferences, cohort access, and the local network.</p></li></ol>${notice('Your Mirror and journal are private from other members. An introduction sees only the portrait you approve. Optional remembered insights and permission to use them for introductions are separate choices.')}<div class="actions">${btn('Begin','create')}${btn('Explore the demonstration','demo','','secondary')}</div></div>`);}
function sections(){return (state.report?.sections||[]).map(s=>J.effective(s,own));}
function reportNav(){return `<div class="mirror-subnav" aria-label="Mirror views">${[['overview','Overview'],['full','Full Mirror'],['conversation','Conversation'],['changes','Changes']].map(([t,l])=>control(l,'mirror-tab',`data-tab="${t}" aria-pressed="${tab===t}"`,true)).join('')}</div>`;}
async function mirror(){if(!state.report){if(state.mode==='demo'){state.report=(await experience({action:'demo_report',name:world.actor==='alex'?'Seer':'Architect'})).report;}else{state.report=(await experience({action:'report'})).report;}}if(!['report','ai'].includes(state.screen))return;const ss=sections(),r=state.report;let body='';
 if(tab==='overview'){const observations=ss.filter(s=>!['strain','changing','uncertainty'].includes(s.id)).slice(0,3);body=`<div class="mirror-overview"><div><p class="eyebrow">${esc(own.basics.name||state.name)} / A working portrait</p><h2>${esc(ss[0]?.body||'Your first Mirror begins with discovery.')}</h2><p class="small muted">${r.evidence?'Based on your discovery self-report.':'An authored sample portrait. Complete discovery for response-based observations.'} You can disagree with every interpretation here.</p></div><aside class="lens-panel">${eye}<p class="eyebrow">An optional lens</p><h3>${['rejected','unassigned'].includes(own.lens)?'No archetype assigned':own.lens==='mixed'||r.lens_status==='mixed'?`${esc(r.name)}${r.secondary?' / '+esc(r.secondary):''}`:'The '+esc(r.name)}</h3><p>Language to try, never an identity to obey.</p>${select('How to use this lens','lens',[['provisional','Keep it provisional'],['mixed','Use a mixed lens'],['unassigned','Leave me unassigned'],['rejected','This lens does not fit']],own.lens)}</aside></div><div class="observations">${observations.map(s=>`<article><p class="eyebrow">${esc(s.label)}</p><h3>${esc(s.title)}</h3><p>${esc(s.body)}</p>${control('Review this insight','section',`data-id="${s.id}"`,true)}</article>`).join('')}</div><section class="quiet-prompt"><p class="eyebrow">An open question</p><h3>${esc(r.prompt)}</h3><div class="actions">${control('Read your full Mirror','mirror-tab','data-tab="full"')}${control('I have reviewed my Mirror','reviewed','','true')}</div></section>`;}
 if(tab==='full'){const s=ss.find(s=>s.id===sectionId)||ss[0];body=`<div class="mirror-reading"><aside><label class="field">Report section<select name="report-section">${ss.map((x,i)=>`<option value="${x.id}" ${x.id===s?.id?'selected':''}>${i+1}. ${esc(x.title)}</option>`).join('')}</select></label><p class="small muted">15 sections. You do not need to accept an archetype to use them.</p>${state.mode==='demo'?`<label class="field">Explore the authored library<select name="report-archetype">${(state.catalog||[]).map(x=>`<option ${x.name===r.name?'selected':''}>${esc(x.name)}</option>`).join('')}</select></label>`:''}</aside><article class="reading-column"><p class="eyebrow">${esc(s?.label)}</p><h2>${esc(s?.title)}</h2>${s?.optional?'<p class="small muted">Optional private material. Open only if useful.</p><details><summary>Explore this section</summary>':''}<p class="mirror-insight">${esc(s?.body)}</p><h4>What supports this</h4><p>${esc(s?.support)}</p><h4>Where it may apply</h4><p>${esc(s?.context)}</p>${s?.label==='A possible pattern'?`<details><summary>The archetypal perspective</summary><p>${esc(s.editorial)}</p></details>`:''}<blockquote>${esc(s?.question)}</blockquote>${s?.optional?'</details>':''}<div class="claim-response"><p class="eyebrow">What did we understand?</p><div class="actions">${[['fits','Fits'],['partly','Partly'],['doesnt','Doesn’t fit'],['unsure','Unsure']].map(([v,l])=>control(l,'correct',`data-fit="${v}" data-id="${s?.id}"`,true)).join('')}</div></div>${control('Explore this with Mirror','explore-claim',`data-id="${s?.id}"`,true)}<div class="actions">${control('I have reviewed my Mirror','reviewed')}</div></article></div>`;}
 if(tab==='changes')body=`<div class="reading-column"><h2>Understanding, revised.</h2><p>Each change is explicit. Corrections replace the claim in your Mirror and are excluded from accepted context unless you confirm the revised wording.</p>${own.history.length?own.history.slice().reverse().map(h=>`<details class="change-item"><summary>${esc(new Date(h.at).toLocaleDateString())} · ${esc(h.claim)} · ${esc(h.fit)}</summary><p><strong>Earlier interpretation:</strong> ${esc(h.original)}</p><p><strong>Your revision:</strong> ${esc(h.revision||h.fit)}</p><p>Introduction use: ${h.matching?'Permitted for the revised statement':'Not permitted'}. Report: ${esc(h.reportVersion)}</p></details>`).join(''):'<p class="notice">No changes yet. Your first correction belongs here.</p>'}</div>`;
 if(tab==='conversation')body=conversation();
 shell('Mirror','A clearer view.<br><em>Room to revise.</em>',reportNav()+body);
 if(state.mode==='demo'&&!state.catalog.length){state.catalog=(await experience({action:'catalog'})).reports;if(tab==='full')return mirror();}
}
function correction(){const p=pendingCorrection;if(!p)return mirror();shell('Review a correction','Your words <em>matter.</em>',`<div class="reading-column"><p class="eyebrow">Original interpretation</p><p>${esc(p.original)}</p><form id="w-correction">${area('What should we understand instead?','revision',p.revision,'maxlength="4000" '+(['partly','doesnt'].includes(p.fit)?'required':''))}${check('Allow this reviewed statement to inform introductions.','matching',false)}<p class="small muted">Your private wording is never displayed in another person’s portrait. Saving pauses new introductions until you review availability.</p><div class="actions"><button class="btn">Preview revision</button>${control('Cancel','report','','true')}</div></form></div>`);}
function correctionPreview(p){pendingCorrection={...pendingCorrection,revision:p.revision,matching:p.matching==='on'};shell('Before applying','Does this say <em>what you mean?</em>',`<div class="reading-column"><p class="eyebrow">Original</p><p>${esc(pendingCorrection.original)}</p><p class="eyebrow">Your correction / proposed replacement</p><blockquote>${esc(p.revision||({fits:'You confirmed this insight.',unsure:'This interpretation remains uncertain and will not be treated as accepted truth.'}[pendingCorrection.fit]))}</blockquote><p>Introduction use: ${pendingCorrection.matching&&own.consent.matching?'Permitted for this revised statement':'Not permitted'}. The earlier claim remains in your private change history.</p><div class="actions">${control('Apply this revision','apply-correction')}${control('Edit my words','edit-correction','','true')}</div></div>`);}
function conversation(){return `<div class="reading-column mirror-conversation"><p class="eyebrow">${state.mode==='demo'?'Written sample responses · No live inference':'Live reflection · Availability depends on configured inference'}</p>${notice('Context: '+(selectedContext?esc(selectedContext.title)+'. '+control('Remove selected context','remove-context','','true'):'Only what you type below. No journal entries, private report, or remembered insights are retrieved automatically.'))}<div class="chat-prompts">${['Explore something in my report','Understand a reaction','Prepare for a conversation','Reflect on a date'].map(t=>control(t,'mirror-prompt',`data-prompt="${t}"`,true)).join('')}</div><div class="mirror-thread" role="log" aria-label="Conversation with Mirror">${mirrorMessages.map(m=>`<article><p class="eyebrow">${m.role==='user'?'You':'WONDER Mirror / '+(state.mode==='demo'?'sample':'reflection')}</p><p>${esc(m.body)}</p></article>`).join('')||'<p>What happened? Begin with what you noticed, before deciding what it meant.</p>'}</div><form id="w-mirror">${area('Your message','message','','required maxlength="6000"')}<p class="small muted">This conversation is not retained as enduring memory. Selected material is processed for this response only; the configured inference service receives it. No other member’s private material is available.</p><button class="btn">${state.mode==='demo'?'Show sample response':'Send to Mirror'}</button></form>${mirrorMessages.length?control('Propose a takeaway to remember','memory','','true'):''}</div>`;}
function memory(source='Mirror conversation'){shell('A possible learning','Keep only <em>what helps.</em>',`<div class="reading-column"><p>This proposal is yours to write or revise. One experience does not establish a permanent trait.</p><form id="w-memory">${area('A takeaway in your words','body','','required maxlength="1500"')}${area('When does this apply? What remains uncertain?','context','','maxlength="1500"')}<input name="source" type="hidden" value="${esc(source)}">${check('Remember this for my private reflection.','remember',false)}${check('Separately allow this insight to inform introductions.','matching',false)}<div class="actions"><button class="btn">Save this takeaway</button>${control('Leave it here','report','','true')}</div></form></div>`);}
function portraitPreview(s){const p=J.shared({...s,portrait:{...s.portrait,approved:true}});return `<article class="approved-portrait">${p.photo?`<img src="${esc(p.photo)}" alt="${esc(p.name)}’s ${state.mode==='demo'?'fictional illustrated':'approved'} portrait">`:'<div class="portrait-placeholder">Photo not added</div>'}<div><p class="eyebrow">${state.mode==='demo'?'Fictional adult · Illustrated portrait':'Connection Portrait'}</p><h2>${esc(p.name)}, ${p.age??'age private'}</h2><p>${esc(p.city)} · ${esc(p.intention)}</p><p class="lede">${esc(p.bio)}</p><h4>An ordinary day I love</h4><p>${esc(p.ordinary)}</p><h4>Something I could talk about for hours</h4><p>${esc(p.interest)}</p><h4>A small thing that makes me feel cared for</h4><p>${esc(p.care)}</p></div></article>`;}
function portrait(preview=false){shell('Connection Portrait',preview?'This is what an introduction <em>can see.</em>':'Choose what <em>you share.</em>',preview?`${portraitPreview(own)}${notice('Audience: the other member in an authorized introduction. Your full birth date, Mirror, corrections, memories, journal, and private reflections are excluded.')}<div class="actions">${control('Approve this portrait','approve-portrait')}${control('Keep editing','portrait','','true')}</div>`:`<div class="reading-column"><p>Your private Mirror and shared portrait are different things. Write in your own voice.</p><form id="w-portrait">${area('A short introduction','bio',own.portrait.bio,'required maxlength="1000"')}${area('An ordinary day I love','ordinary',own.portrait.ordinary,'required maxlength="600"')}${area('Something I could talk about for hours','interest',own.portrait.interest,'maxlength="600"')}${area('A small thing that makes me feel cared for','care',own.portrait.care,'maxlength="600"')}<p class="small muted">${state.mode==='demo'?'This demonstration uses clearly labeled illustrated adults.':'Photo upload and moderation must be connected before a real portrait can be activated. Your text can be saved privately now.'}</p><button class="btn">Save and preview</button></form></div>`);}
function viewPair(){if(state.mode!=='demo')return pair;if(['empty','failed'].includes(scenario))return null;if(['blocked','unavailable'].includes(scenario))return {status:'unavailable'};if(scenario==='declined')return {status:'declined'};return J.pairView(world.pair,world.actor,world.users);}
function waiting(title,copy){return `<div class="waiting-state"><p class="eyebrow">Introduction status</p><h2>${title}</h2><p>${copy}</p><div class="actions">${control('Return to your Mirror','report')}${control('Review preferences','settings','','true')}</div></div>`;}
function introductions(){const p=viewPair();let body='';if(scenario==='failed')body=waiting('We could not load your introduction.','This is a simulated service failure, not a matching decision. Change the demo scenario to retry.');else if(!own.portrait.approved)body=waiting('Your portrait is not ready.','Choose and approve what another person can see before becoming available.');else if(own.availability!=='available')body=waiting('Introductions are paused.','Your Mirror and journal remain available. Resume only when you want to.');else if(!p)body=waiting('No introduction is ready yet.',state.mode==='demo'?'Your preferences are saved. The empty-state scenario contains no eligible person.':'Live introductions are awaiting cohort activation and the release checks. This does not affect access to your private Mirror.');else if(['unavailable','withdrawn','declined','closed','blocked'].includes(p.status))body=waiting('This introduction is no longer active.','You do not need to explain or manufacture a connection. Closed and blocked pairs are not recycled automatically.');else{const other=state.mode==='demo'?world.users[world.pair.members.find(x=>x!==world.actor)]:null;body=`${other?portraitPreview(other):p.otherPortrait?`<article class="reading-column"><h2>${esc(p.otherPortrait.name)}, ${p.otherPortrait.age}</h2><p>${esc(p.otherPortrait.city)} · ${esc(p.otherPortrait.intention)}</p><p>${esc(p.otherPortrait.bio)}</p></article>`:''}<section class="introduction-reasons"><h3>Why we thought of you together</h3>${(p.reason||[]).map(t=>`<p>${esc(t)}</p>`).join('')}<h4>A difference worth noticing</h4><p>${esc(p.difference)}</p><h4>What only meeting can tell you</h4><p>${esc(p.unknown)}</p><p class="small muted">${state.mode==='demo'?'This rationale uses the two fictional approved portraits. It is not a prediction.':'Reasons use information authorized for this introduction.'}</p></section>`;
 if(p.status==='proposed'||p.status==='pending'){body+=p.myInterest?notice('Your interest is recorded. Contact remains closed until the other person chooses independently.'):`<div class="actions">${control('I’m interested','interest')}${control('Not for me','decline','','true')}${control('Decide later','later','','true')}</div>`;if(state.mode==='demo')body+=notice('To demonstrate mutuality, switch to '+(world.actor==='alex'?'Rowan':'Alex')+' in Demonstration controls and make their separate choice.');}
 if(p.status==='mutual'){body+=`<section class="human-conversation"><p class="eyebrow">You are both interested / Human conversation</p><h2>A conversation <em>between you.</em></h2><p class="small muted">${state.mode==='demo'?'Messages are simulated. Switch perspectives to write as the other fictional adult. No AI sends messages for either person.':'Only the two participants can use this conversation.'}</p><div class="human-thread" role="log" aria-label="Conversation with your introduction">${(p.messages||[]).map(m=>`<article class="${m.from===(world?.actor||state.user?.id)?'mine':''}"><p class="eyebrow">${esc(state.mode==='demo'?world.users[m.from].basics.name:m.from===state.user?.id?'You':'Your introduction')}</p><p>${esc(m.body)}</p><span class="small muted">${state.mode==='demo'?'Saved in demo':'Sent'}</span></article>`).join('')||'<p>No messages yet. Something you are curious about is a good beginning.</p>'}</div><form id="w-message">${area('Message to this person','body','','required maxlength="3000"')}<button class="btn">${state.mode==='demo'?'Send simulated message':'Send message'}</button></form></section>${planView(p)}`;}
 body+=`<div class="actions safety-actions">${control('Block this person','block','','true')}${control('Report a concern','report-person','','true')}${control('Close this connection','close','','true')}</div>`;}
 shell('Introductions','A person.<br><em>A reason to be curious.</em>',body);}
function planView(p){const plan=p.plan;return `<section class="meeting-plan"><p class="eyebrow">Plan to meet</p><h3>${plan?'Your meeting plan':'Make room in the real world.'}</h3>${plan?`<div class="notice"><strong>${esc(plan.status==='confirmed'?'Confirmed':plan.status==='cancelled'?'Cancelled':'Proposed — awaiting the other person')}</strong><p>${esc(plan.when.replace('T',' at '))} · ${esc(plan.zone)}</p><p>${esc(plan.place)}</p><p>${esc(plan.budget)} ${esc(plan.access)}</p><div class="actions">${plan.status==='proposed'&&plan.by!==(world?.actor||state.user?.id)?control('Accept this plan','accept-plan'):''}${plan.status==='confirmed'?control('Export calendar event','calendar','','true'):''}${plan.status!=='cancelled'?control('Cancel this plan','cancel-plan','','true'):''}</div></div>`:''}<details><summary>${plan?'Propose another time':'Propose a day, time, and public place'}</summary><form id="w-plan" class="form-grid">${field('Day and time','when',plan?.when||'','datetime-local','required')}${field('Time zone','zone',plan?.zone||'America/Chicago','text','required')}${field('Public location or activity','place',plan?.place||'','text','required maxlength="300"')}${field('Budget preference (optional)','budget','','text','maxlength="200"')}${field('Access needs to share (optional)','access','','text','maxlength="300"')}<div class="full"><p class="small muted">No reservation is made. Reminders are not connected in this build. Share only what you want the other person to see.</p><button class="btn">Send proposal</button></div></form></details><p class="small muted">Choose a public place and your own way home. WONDER does not monitor meetings or provide emergency response.</p>${plan?.status==='confirmed'?`<div class="quiet-prompt"><h4>What would help you be present rather than perform?</h4><p>Ask about an ordinary day they love, or something they recently changed their mind about.</p>${control('Record whether you met','reflection')}</div>`:''}</section>`;}
function reflect(attendance=''){const latest=own.reflections[0];shell('After the meeting','Experience, <em>before interpretation.</em>',`<div class="reading-column"><p>Your reflection is private. No answer here sends a message or changes the other person’s status.</p><form id="w-reflection">${select('Did you meet?','attendance',[['','Choose…'],['yes','Yes'],['rescheduled','Rescheduled'],['cancelled','Cancelled'],['no','It did not happen'],['private','Prefer not to say']],attendance)}${attendance==='yes'?['feeling','authentic','understood','attraction','again'].map((k,i)=>select(['Did being together feel comfortable?','Did you feel able to be yourself?','Did you feel listened to and understood?','Was there attraction or interest?','Would you like another meeting?'][i],k,[['unsure','Not sure yet'],['yes','Yes'],['partly','Partly'],['no','No']],'unsure')).join(''):''}${attendance?area('Anything you want to remember? (optional)','body','','maxlength="8000"'):''}<div class="actions">${attendance?'<button class="btn">Save privately</button>':''}${control('Skip for now','home','','true')}</div></form>${latest?`<details><summary>Your latest private reflection</summary><p>${esc(latest.attendance)}</p><p>${esc(latest.body)}</p></details>`:''}<div class="actions">${control('Propose a learning update','reflection-memory','','true')}${control('I need support / report a concern','report-person','','true')}</div></div>`);}
function journal(){writeDraft=writeDraft||{id:crypto.randomUUID(),title:'',body:'',tags:''};const d=writeDraft;shell('Journal','A private place<br><em>to put things into words.</em>',`<div class="journal-layout"><aside><label class="field">Search entries<input id="w-search" type="search" placeholder="Title, words, or tags"></label>${control('New entry','new-journal','','true')}<div id="w-entry-list">${entryList()}</div>${control('Export my journal','export-journal','','true')}</aside><section><form id="w-journal">${field('Title (optional)','title',d.title,'text','maxlength="160"')}${area('Your writing','body',d.body,'maxlength="12000" placeholder="When did you feel most understood today?"')}${field('Tags (optional)','tags',d.tags,'text','maxlength="200"')}<div class="journal-bottom"><span id="w-save-state" role="status">${dirty?'Not saved':'Saved entries are private'}</span><button class="btn">Save now</button></div></form><p class="small muted">No AI reads this entry automatically. Autosave runs after you pause typing. Export is yours to keep privately.</p><div class="actions">${control('Reflect on selected text','reflect-selection','','true')}${control('Reflect on this entry','reflect-entry','','true')}${control('Delete entry','delete-journal','','true')}</div></section></div>`);}
function entryList(query=''){return own.journal.filter(e=>(e.title+' '+e.body+' '+e.tags).toLowerCase().includes(query.toLowerCase())).map(e=>`<button class="entry-link" data-w-action="open-journal" data-id="${esc(e.id)}"><span class="eyebrow">${esc(new Date(e.at).toLocaleDateString())}</span><strong>${esc(e.title)}</strong><span class="small muted">${esc(e.tags)}</span></button>`).join('')||'<p class="small muted">No saved entries here yet.</p>';}
async function saveJournal(){clearTimeout(saveTimer);const data={...writeDraft};if(!data.body.trim())return;const indicator=document.querySelector('#w-save-state');if(indicator)indicator.textContent='Saving';const work=saveChain.catch(()=>{}).then(async()=>{await change('journal',data);if(writeDraft?.id===data.id&&writeDraft.body===data.body&&writeDraft.title===data.title)dirty=false;const el=document.querySelector('#w-save-state');if(el)el.textContent=dirty?'Not saved':'Saved';const list=document.querySelector('#w-entry-list');if(list)list.innerHTML=entryList();});saveChain=work;try{await work;}catch(e){const el=document.querySelector('#w-save-state');if(el)el.textContent='Not saved — '+e.message;throw e;}}
function account(){shell('Account','Your portrait.<br><em>Your decisions.</em>',`<div class="account-grid"><section><h3>${esc(own.basics.name||'Your account')}</h3><p>${esc(own.basics.city||'Add a city')} · ${state.mode==='demo'?'Fictional adult account':'Private member account'}</p><div class="actions">${control('Edit essentials','basics','','true')}${control('Connection Portrait','portrait','','true')}</div><form id="w-preferences">${select('Relationship intention','intention',[['','Choose…'],['A committed relationship','A committed relationship'],['Meaningful dating','Meaningful dating'],['Self-understanding only','Self-understanding only']],own.preferences.intention)}<div class="form-grid">${field('Required minimum age','minAge',own.preferences.minAge,'number','min="18" max="120" required')}${field('Required maximum age','maxAge',own.preferences.maxAge,'number','min="18" max="120" required')}</div>${check('Same broad city is required.','cityRequired',own.preferences.cityRequired)}<p class="small muted">Changing requirements pauses new introductions. Existing screens are checked again before actions.</p><button class="btn">Save required preferences</button></form><h3>City access</h3><p>${state.mode==='demo'?'This demonstration takes place in Dallas.':'Mirror access does not imply access to a live local introduction cohort.'}</p>${own.cityInterest?notice('City interest saved for '+esc(own.cityInterest.city)+'. No position or access date is promised. Notifications are not connected in this development build.') :''}<div class="actions">${control(own.cityInterest?'Withdraw city interest':'Save interest in my city',own.cityInterest?'withdraw-city':'join-city','','true')}</div><h3>Availability</h3><p>${own.availability==='seeing'?'You are seeing someone. This is not announced to anyone.':own.availability==='paused'?'New introductions are paused.':'Available for eligible introductions.'}</p><div class="actions">${control('Resume introductions','resume','','true')}${control('Pause introductions','pause','','true')}${control('I’m seeing someone','seeing','','true')}</div></section><section><h3>Privacy and permissions</h3><form id="w-consent">${check('Process my discovery to provide my private Mirror.','process',own.consent.process)}${check('Use eligible, permitted information for introductions.','matching',own.consent.matching)}<p class="small muted">Research and model-improvement use is off and unavailable. Your journal is excluded unless you select material to reflect on. Withdrawing processing pauses introductions and removes optional memories.</p><button class="btn">Save permissions</button></form><h3>Remembered insights</h3>${own.memories.length?own.memories.map(m=>`<article class="memory"><p>${esc(m.body)}</p><p class="small">${esc(m.context)} · ${m.matching?'Introduction use permitted':'Private reflection only'}</p>${control('Forget this insight','forget',`data-id="${esc(m.id)}"`,true)}</article>`).join(''):'<p>No optional insights remembered.</p>'}<h3>Reading preferences</h3><form id="w-display">${check('Larger text','largeText',own.display.largeText)}${check('Reduced motion','reducedMotion',own.display.reducedMotion)}<button class="btn secondary">Save reading preferences</button></form><h3>Data and departure</h3><p>There are no charges or subscriptions in this build.</p><div class="actions">${control('Export my private data','export','','true')}${control(state.mode==='demo'?'Delete this fictional account':'Delete my account','delete-account','','true')}${btn(state.mode==='demo'?'Exit demo':'Sign out','logout','','secondary')}</div><p class="small muted">${state.mode==='demo'?'Demo deletion clears this actor’s private records and closes the simulated connection. Reset can recreate the fictional fixtures.':'Real account deletion requires the deployment’s complete deletion procedure. If unavailable, the action will explain the blocker before changing anything.'}</p></section></div>`);}
async function render(screen){if(screen==='how'){how();return;}await load();if(own.deleted){shell('Account deleted','You have <em>left the demonstration.</em>',`<p>Your active private records and connection access were removed.</p>${control('Reset fictional data','reset')}`);return;}
 if(state.mode==='demo'){world.reports=world.reports||{};world.assessments=world.assessments||{};if(state.report)world.reports[world.actor]=state.report;if(state.assessment)world.assessments[world.actor]=state.assessment;storeDemo();}
 if(screen==='home')home();else if(screen==='basics')basics();else if(screen==='report'||screen==='ai'){if(screen==='ai')tab='conversation';await mirror();}else if(screen==='introductions')introductions();else if(screen==='reflection')reflect();else if(screen==='journal')journal();else if(screen==='portrait')portrait();else if(screen==='settings')account();}
async function dispatch(a,b){if(['home','report','introductions','journal','settings','portrait','basics','reflection'].includes(a)){pendingCorrection=null;return go(a);}if(a==='mirror-tab'){tab=b.dataset.tab;return mirror();}if(a==='section'){sectionId=b.dataset.id;tab='full';return mirror();}if(a==='correct'){const s=sections().find(x=>x.id===b.dataset.id);pendingCorrection={claim:s.id,fit:b.dataset.fit,original:s.body,revision:'',reportVersion:state.report.version};return correction();}if(a==='edit-correction')return correction();if(a==='apply-correction'){await change('correct',pendingCorrection);pendingCorrection=null;tab='changes';return mirror();}if(a==='reviewed'){await change('review_mirror');toast('Your review is saved.');return go('portrait');}if(a==='explore-claim'){const s=sections().find(x=>x.id===b.dataset.id);selectedContext={title:s.title,text:s.body};tab='conversation';return mirror();}if(a==='remove-context'){selectedContext=null;return mirror();}if(a==='mirror-prompt'){document.querySelector('#w-mirror [name=message]').value=b.dataset.prompt;return;}if(a==='memory')return memory();if(a==='reflection-memory')return memory('Private date reflection');if(a==='approve-portrait'){await change('approve_portrait');return go('settings');}
 if(['interest','decline','later','close'].includes(a)){await connect(a);if(a==='later')return go('home');return introductions();}if(a==='accept-plan'){await connect('accept_plan');return introductions();}if(a==='cancel-plan'){if(await confirm('Cancel this plan?','The other person will see that the plan is cancelled.')){await connect('cancel_plan');introductions();}return;}if(a==='block'){if(!await confirm('Block this person?','Contact closes immediately. This pair cannot be introduced again. A report is optional.'))return;if(state.mode==='demo'){const other=world.pair.members.find(x=>x!==world.actor);await connect('block');await change('block',{userId:other});}else await connect('block');return introductions();}if(a==='report-person')return shell('Support','Make a concern <em>clear.</em>',`<div class="reading-column">${notice(state.mode==='demo'?'This is a local demonstration. No moderator will receive this report.':'Moderation delivery is not active in this development build. Do not rely on this form for urgent help.')}<form id="w-report">${area('What happened?','reason','','required maxlength="3000"')}<button class="btn">${state.mode==='demo'?'Save simulated report':'Save private concern'}</button></form><p>For immediate danger, contact local emergency services. Blocking does not require a report.</p></div>`);
 if(a==='calendar'){const p=viewPair().plan;if(p?.status!=='confirmed')throw new Error('Only a confirmed plan can be exported.');const safe=x=>x.replace(/[\r\n]/g,' ').replace(/[,;]/g,' ');download('WONDER-meeting.ics',`BEGIN:VCALENDAR\r\nVERSION:2.0\r\nPRODID:-//WONDER//Meeting//EN\r\nBEGIN:VEVENT\r\nUID:${pair.id}@wonder\r\nDTSTAMP:${new Date().toISOString().replace(/[-:]/g,'').split('.')[0]}Z\r\nDTSTART;TZID=${safe(p.zone)}:${p.when.replace(/[-:]/g,'')}00\r\nSUMMARY:WONDER meeting\r\nLOCATION:${safe(p.place)}\r\nEND:VEVENT\r\nEND:VCALENDAR\r\n`,'text/calendar');return;}
 if(a==='new-journal'){if(dirty)await saveJournal();writeDraft=null;return journal();}if(a==='open-journal'){if(dirty)await saveJournal();writeDraft={...own.journal.find(e=>e.id===b.dataset.id)};return journal();}if(a==='delete-journal'){if(!await confirm('Delete this entry?','The saved entry and active selected context will be removed.'))return;clearTimeout(saveTimer);await saveChain.catch(()=>{});await change('delete_journal',{id:writeDraft.id});selectedContext=null;writeDraft=null;dirty=false;return journal();}if(a==='export-journal')return download('WONDER-private-journal.json',own.journal);
 if(a==='reflect-entry'||a==='reflect-selection'){const input=document.querySelector('#w-journal [name=body]');const text=a==='reflect-selection'?input.value.slice(input.selectionStart,input.selectionEnd):input.value;if(!text.trim())throw new Error(a==='reflect-selection'?'Select the excerpt you want to use first.':'Write an entry first.');if(!await confirm('Use this selected material with Mirror?',state.mode==='demo'?'This excerpt will be shown beside a clearly labeled sample response. It is not saved as a memory.':'Only this selected material will be sent to the configured inference service when you send your next message. It will not be remembered automatically.'))return;selectedContext={title:a==='reflect-selection'?'Selected journal excerpt':'Selected journal entry',text,sourceId:writeDraft.id};tab='conversation';return go('report');}
 if(['resume','pause','seeing'].includes(a)){if(a==='resume'&&state.mode!=='demo'&&!liveEnabled)throw new Error('Real introductions remain paused until cohort, portrait moderation, and release verification are connected.');await change('availability',{value:{resume:'available',pause:'paused',seeing:'seeing'}[a]});return account();}if(a==='join-city'||a==='withdraw-city'){await change('city_interest',{active:a==='join-city'});return account();}if(a==='forget'){await change('forget',{id:b.dataset.id});selectedContext=null;mirrorMessages=[];return account();}if(a==='export')return download('WONDER-private-export.json',state.mode==='demo'?{journey:own,report:state.report,assessment:state.assessment}:await api('/api/journey',{action:'export'}));if(a==='delete-account'){if(!await confirm('Delete this account?',state.mode==='demo'?'This erases this fictional adult’s private data and closes their connection.':'This permanently removes your account under the disclosed retention policy.'))return;if(state.mode==='demo'){await change('delete');world.pair.status='withdrawn';world.pair.messages=[];world.pair.plan=null;world.pair.reminders=[];storeDemo();mirrorMessages=[];selectedContext=null;writeDraft=null;state.report=null;return go('settings');}await api('/api/journey',{action:'delete_account'});return ctx.logout();}
 if(a==='switch'){if(dirty)await saveJournal();world.reports=world.reports||{};world.assessments=world.assessments||{};world.reports[world.actor]=state.report;world.assessments[world.actor]=state.assessment;world.actor=world.actor==='alex'?'rowan':'alex';storeDemo();owner=null;own=null;writeDraft=null;selectedContext=null;mirrorMessages=[];state.report=null;state.assessment=null;await load();return go('home');}if(a==='reset'||a==='fresh'){if(!await confirm('Reset this demonstration?','This clears fictional writing, messages, and changes in this tab only.'))return;world=J.fixture();if(a==='fresh'){world.users.alex=J.initial();state.assessment=null;state.report=null;}storeDemo();owner=null;own=null;writeDraft=null;selectedContext=null;mirrorMessages=[];state.report=null;state.assessment=null;state.entries=[];state.messages=[];await load();return go(a==='fresh'?'basics':'home');}
}
const forms={
 'w-basics':async p=>{await change('basics',{...p,process:p.process==='on'});state.name=own.basics.name;return go('assessment');},
 'w-correction':async p=>correctionPreview(p),
 'w-memory':async p=>{await change('memory',{...p,id:crypto.randomUUID(),sourceId:selectedContext?.sourceId,remember:p.remember==='on',matching:p.matching==='on'});return go('settings');},
 'w-portrait':async p=>{await change('portrait',{...p,photo:own.portrait.photo});return portrait(true);},
 'w-preferences':async p=>{await change('preferences',{...p,cityRequired:p.cityRequired==='on'});toast('Saved. Review availability before resuming.');return account();},
 'w-consent':async p=>{await change('consent',{process:p.process==='on',matching:p.matching==='on'});selectedContext=null;mirrorMessages=[];return account();},
 'w-display':async p=>{await change('display',{largeText:p.largeText==='on',reducedMotion:p.reducedMotion==='on'});return account();},
 'w-message':async p=>{await connect('message',{...p,id:crypto.randomUUID()});return introductions();},
 'w-plan':async p=>{await connect('plan',p);return introductions();},
 'w-reflection':async p=>{if(!p.attendance)throw new Error('Choose whether you met.');await change('reflection',{...p,id:crypto.randomUUID(),pairId:pair?.id,answers:p});toast('Saved privately. Nothing was sent to your date.');return memory('Private date reflection');},
 'w-report':async p=>{await change('report',{...p,userId:pair?.members?.find(x=>x!==(world?.actor||state.user?.id))});toast(state.mode==='demo'?'Simulated report saved.':'Private concern saved; moderator delivery is unavailable.');return go('settings');},
 'w-journal':async()=>{await saveJournal();},
 'w-mirror':async p=>{let reply;if(state.mode==='demo')reply='Written sample: What did you directly notice, and what did you infer from it? Those may tell different stories. Name one concrete moment before deciding whether it describes a broader pattern.';else reply=(await api('/api/chat',{message:p.message,context:selectedContext?{label:selectedContext.title,text:selectedContext.text}:null,process_context:!!selectedContext})).reply;mirrorMessages.push({role:'user',body:p.message},{role:'assistant',body:reply});return mirror();}
};
document.addEventListener('click',e=>{const b=e.target.closest('[data-w-action]');if(!b||b.disabled)return;e.preventDefault();e.stopImmediatePropagation();b.disabled=true;dispatch(b.dataset.wAction,b).catch(x=>error(x.message)).finally(()=>b.disabled=false);},true);
document.addEventListener('submit',e=>{const fn=forms[e.target.id];if(!fn)return;e.preventDefault();e.stopImmediatePropagation();const b=e.target.querySelector('[type=submit],button:not([type])');if(b?.disabled)return;if(b)b.disabled=true;const data=Object.fromEntries(new FormData(e.target));fn(data).catch(x=>error(x.message)).finally(()=>{if(b)b.disabled=false;});},true);
document.addEventListener('input',e=>{if(e.target.id==='w-search'){document.querySelector('#w-entry-list').innerHTML=entryList(e.target.value);return;}if(e.target.closest('#w-journal')){writeDraft[e.target.name]=e.target.value;dirty=true;document.querySelector('#w-save-state').textContent='Not saved';clearTimeout(saveTimer);saveTimer=setTimeout(()=>saveJournal().catch(()=>{}),900);}});
document.addEventListener('change',e=>{const name=e.target.name;if(name==='report-section'){sectionId=e.target.value;mirror().catch(x=>error(x.message));}if(name==='report-archetype'){state.report=state.catalog.find(x=>x.name===e.target.value);mirror().catch(x=>error(x.message));}if(name==='lens')change('lens',{value:e.target.value}).then(()=>mirror()).catch(x=>error(x.message));if(name==='attendance'){const v=e.target.value;reflect(v);}if(name==='demo-scenario'){scenario=e.target.value;world.scenario=scenario;if(scenario==='cancelled'){world.pair.status='mutual';world.pair.interest={alex:'interested',rowan:'interested'};world.pair.plan={when:'2026-10-02T16:00',zone:'America/Chicago',place:'Public gallery (fictional plan)',by:'alex',status:'cancelled'};world.pair.reminders=[];}storeDemo();go('introductions');}});
window.addEventListener('beforeunload',e=>{if(dirty){e.preventDefault();e.returnValue='';}});
return {render,nav,load,handles:s=>['home','report','ai','introductions','journal','reflection','settings','portrait','basics','how'].includes(s),beforeQuiz:async()=>{await load();if(!own.consent.process||J.age(own.basics.dob)<18||J.age(own.basics.dob)===null){await go('basics');return false;}return true;},flush:async()=>{if(dirty)await saveJournal();},clear:()=>{owner=null;own=null;writeDraft=null;selectedContext=null;mirrorMessages=[];dirty=false;clearTimeout(saveTimer);}};
};
